// Export all custom hooks for VoiceCode

// Mobile and responsive hooks
export { 
  useResponsive, 
  useIsMobile, 
  useSafeArea, 
  useViewportHeight, 
  useReducedMotion, 
  useDevicePixelRatio 
} from './use-mobile';

// AI Processing hooks
export { useAIWorker } from './useAIWorker';